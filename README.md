# Site Polloni Flow

Landing page em Django para a empresa Polloni Flow.

## Rodar localmente

```powershell
python -m pip install -r requirements.txt
python manage.py runserver
```

Acesse:

```text
http://127.0.0.1:8000/
```
